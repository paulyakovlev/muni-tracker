apiVersion: 1

datasources:
  - name: Muni Postgres
    type: postgres
    url: postgres.muni.svc.cluster.local:5432
    database: muni
    user: muni
    secureJsonData:
      password: munipass
    jsonData:
      sslmode: disable
      database: muni
    isDefault: true
